git_url(ENV['MATCH_GIT_URL']) if ENV['MATCH_GIT_URL']

storage_mode('git')
type('appstore')
app_identifier([ENV['IOS_BUNDLE_IDENTIFIER'] || 'com.fermatolk.mobile'])

username(ENV['APPLE_ID']) if ENV['APPLE_ID']
team_id(ENV['APPLE_TEAM_ID']) if ENV['APPLE_TEAM_ID']
